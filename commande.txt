RMQ: pas les mm conventions (y compris de nommage (_)) ds les tests vs code de prod
mettre des enums stragegies?

----------application cmd/macro/undo/redo
appli exemple undo/redo: 
		-soit tt se passe ds un mm process, auquel cas c'est soit client lourd soit il faut passer au JS
		-soit 2 process, auquel cas, il faut maintenier une conversation:
				-plus difficile ds me cas JPA
				-donc pas JPA, ou se faire aider par techno genre spring flow/CDI
		 
-----conversation------
(chaque commande->allerretour servur car il nous renvoie des infos,
 fin conversation-->commit donc necessaire tenir conversation et em/transaction/autre cote serveur)

conversation: composition d'une voiture avec options?
voyage? etape1, choix options, ecran confirmation avec undo/redos? 
jeu par tour?
actions CRUD jpa/nosql/REST, aller retour neccesaire car recherche depend de l'exec des cmds precedentes
		-->cru voir supprt  scpeifique jpa persistence ctx etendu

2.2.1. Embedding A Flow On A Page

By default Web Flow does a client-side redirect upon entering every view state. That makes it impossible to embed a flow on a page or within a modal dialog and execute more than one view state without causing a full-page refresh. Web Flow now supports launching a flow in "embedded" mode. In this mode a flow can transition to other view states without a client-side redirect during Ajax requests. See Section 11.7, “Embedding A Flow On A Page” and Section 13.8, “Embedding a Flow On a Page”. 

variable de flow pour tenir lma conversation?
 		Use the var element to declare a flow variable:
		<var name="searchCriteria" class="com.mycompany.myapp.hotels.search.SearchCriteria"/>
		Make sure your variable's class implements java.io.Serializable, as the instance state is saved between flow requests. 